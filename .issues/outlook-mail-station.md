## outlook-mail-station 实施计划

1. 创建独立项目 `outlook-mail-station/`，使用 `uv + pyenv`，不依赖当前项目运行时。
2. 实现 Outlook 邮箱导入、列表维护、收件同步、发件发送、临时授权分享后端。
3. 实现独立前端站点：左侧邮箱列表，右侧当前邮箱与收件区，支持导入、发信、刷新、临时授权。
4. 完成构建与基础联调，确保新项目可单独启动。
